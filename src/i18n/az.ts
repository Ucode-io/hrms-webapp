import type { TKey } from './index'

/**
 * Азербайджанский заводится пустым намеренно (ADR-0006).
 *
 * Язык должен существовать раньше перевода: он выбирается на регионе, приходит
 * из Telegram и лежит в словаре `languages` — значение, которого приложение не
 * знает, молча откатилось бы в русский и выглядело бы как поломка. `translate()`
 * подставляет русский по каждому отсутствующему ключу, а `DICTS` объявлен
 * `Partial` — пустой словарь здесь заложенный сценарий, а не заготовка.
 */
export const az: Partial<Record<TKey, string>> = {}
